'use client';
// SEO component — no-op in Next.js App Router; metadata is handled per-page via generateMetadata
// Kept for component compatibility during migration
export default function SEO() { return null; }
